---
title: 'Forgot Password'
description: 'Endpoint to handle password reset requests'
---

# Forgot Password

Request a password reset for a user account.

## Request

<ParamField body="email" type="string" required>
  Email address of the account that needs password reset
</ParamField>

```bash
POST /api/authenticate/forgot-password
Content-Type: application/json

{
  "email": "user@example.com"
}
```

## Response

<ResponseField name="success" type="boolean">
  Indicates if the password reset request was successful
</ResponseField>

<ResponseField name="message" type="string">
  Status message about the password reset request
</ResponseField>

```json
{
  "success": true,
  "message": "Password reset instructions sent to your email"
}
```

<ResponseExample>
```json
{
  "success": true,
  "message": "Password reset instructions sent to your email"
}
```
</ResponseExample>